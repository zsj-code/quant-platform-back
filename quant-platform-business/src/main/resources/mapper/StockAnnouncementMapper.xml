<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quant.platform.business.stock.mapper.StockAnnouncementMapper">

    <insert id="upsertBatch">
        INSERT INTO stock_announcement (
            id, code, symbol, source, external_id, title, announce_time, notice_date, categories, fetched_at
        )
        VALUES
        <foreach collection="list" item="e" separator=",">
            (
                #{e.id}, #{e.code}, #{e.symbol}, #{e.source}, #{e.externalId}, #{e.title}, #{e.announceTime},
                #{e.noticeDate}, #{e.categories}, #{e.fetchedAt}
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
            code = VALUES(code),
            symbol = VALUES(symbol),
            title = VALUES(title),
            announce_time = VALUES(announce_time),
            notice_date = VALUES(notice_date),
            categories = VALUES(categories),
            fetched_at = VALUES(fetched_at)
    </insert>
</mapper>
